--- 
applications: 
  .: 
    instances: 1
    framework: 
      name: standalone
      info: 
        exec: 
        description: Standalone Application
        mem: 64M
    name: enlive-by-example
    runtime: java7
    mem: 512M
    command: java $JAVA_OPTS -cp enlive-by-example.jar example.enlive.web
    url: ${name}.${target-base}
