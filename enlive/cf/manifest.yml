--- 
applications: 
  .: 
    instances: 1
    framework: 
      name: standalone
      info: 
        exec: 
        description: Standalone Application
        mem: 64M
    name: enlive-by-example
    runtime: java7
    mem: 512M
    command: java $JAVA_OPTS -cp clojure-by-example-1.0.0-SNAPSHOT-standalone.jar web.select
    url: ${name}.${target-base}
